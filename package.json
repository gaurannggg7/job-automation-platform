{
  "name": "job-automation-platform",
  "version": "1.0.0",
  "description": "AI-powered job application and networking automation",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/web\" \"npm run dev --workspace=apps/api\"",
    "build": "npm run build --workspace=apps/web && npm run build --workspace=apps/api",
    "test": "npm run test --workspace=apps/web && npm run test --workspace=apps/api",
    "setup": "npm install && npm run setup --workspace=apps/web && npm run setup --workspace=apps/api"
  },
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
